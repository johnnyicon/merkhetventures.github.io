# Merkhet World — One-shot Prompt

```text
Use the installed `$scroll-world` skill to turn the Merkhet Ventures content in `content/` and `brand/` into a world. Create the result inside `apps/merkhet-world/`.
```
